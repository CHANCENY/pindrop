CREATE TABLE IF NOT EXISTS `site_settings` (id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY, key_token VARCHAR(300) NOT NULL, content TEXT NULL, created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP)